SMT-slover:
sudo apt-get install cvc4

AstroVer:
https://forge.ispras.ru/projects/astraver/wiki

Alternative cmnds:
`opam init -c 4.11.2`
`opam install coq coqide astraver --ignore-conflicts --update-invariant`

After install:
why3 config --detect


Run:
frama-c -av /path/to/main.c
